# Implementation Plan

- [ ] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Dashboard Stats Missing Fields and Attendance Missing Student Names
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For deterministic bugs, scope the property to the concrete failing case(s) to ensure reproducibility
  - Test implementation details from Bug Condition in design:
    - Call `/dashboard/stats` endpoint and verify response is missing `weeklyAttendance`, `monthlyAttendanceRate`, `presentVsAbsent`, `totalSessionsToday`, `presentToday`, `absentToday`, `attendancePercentage` fields
    - Call `/attendance/` endpoint and verify response records are missing `student_name` field
  - The test assertions should match the Expected Behavior Properties from design:
    - Dashboard stats should include all 10+ required fields with correct data types
    - Attendance records should include `student_name` field with full name (first_name + last_name)
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause:
    - Dashboard stats response contains only 4 fields instead of 10+ fields
    - Attendance records contain only `student_id` without `student_name`
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [ ] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Existing CRUD Operations and Authentication
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs:
    - Test GET `/students/` returns complete list of students with all existing fields
    - Test GET `/classes/` returns complete list of classes with all existing fields
    - Test GET `/sessions/` returns complete list of sessions with all existing fields
    - Test POST `/attendance/` successfully creates attendance records
    - Test GET `/attendance/export` generates and returns CSV file
    - Test authentication endpoints (`/auth/login`, `/auth/signup`, `/auth/me`) function correctly
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - For all student CRUD operations, verify existing behavior is unchanged
    - For all class CRUD operations, verify existing behavior is unchanged
    - For all session CRUD operations, verify existing behavior is unchanged
    - For all attendance POST operations, verify records are created successfully
    - For all authentication operations, verify JWT token validation works correctly
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [ ] 3. Fix for hardcoded data removal in dashboard stats and attendance endpoints

  - [ ] 3.1 Update `backend/app/crud.py` - Enhance `get_dashboard_stats` function
    - Add today's date calculation using `datetime.now().date()`
    - Add today's metrics queries:
      - Query sessions where `date.date() == today`
      - Count total sessions today
      - Query attendance logs for today's sessions
      - Count present and absent statuses
      - Calculate attendance percentage: `(present / (present + absent)) * 100` if total > 0, else 0
    - Add weekly attendance calculation:
      - Query attendance logs for the last 7 days
      - Group by date and status
      - Count present and absent for each day
      - Format as `[{name: "Mon", present: X, absent: Y}, ...]`
      - Use day names (Mon, Tue, Wed, Thu, Fri, Sat, Sun)
    - Add monthly attendance rate calculation:
      - Query attendance logs for the last 4 weeks
      - Group by week number
      - Calculate attendance rate for each week: `(present / total) * 100`
      - Format as `[{name: "Week 1", rate: X}, ...]`
    - Add present vs absent calculation:
      - Use today's attendance data already queried
      - Format as `[{name: "Present", value: X}, {name: "Absent", value: Y}]`
    - Return complete stats dictionary with all fields
    - _Bug_Condition: isBugCondition(request) where request.endpoint == "/dashboard/stats" AND response.missing("weeklyAttendance", "monthlyAttendanceRate", "presentVsAbsent", "totalSessionsToday", "presentToday", "absentToday", "attendancePercentage")_
    - _Expected_Behavior: expectedBehavior(response) from design - response includes all 10+ required fields with correct data types_
    - _Preservation: Preservation Requirements from design - all other CRUD operations remain unchanged_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [ ] 3.2 Update `backend/app/crud.py` - Enhance `get_attendance` function
    - Add Student join using SQLAlchemy:
      - Change query from `db.query(models.AttendanceLog)` to include join with `models.Student`
      - Use `joinedload` or eager loading to avoid N+1 queries
    - Add student name to response:
      - After querying, iterate through results
      - For each attendance log, access `attendance.student.first_name` and `attendance.student.last_name`
      - Construct full name and add to response dictionary
      - Handle case where student might be None (deleted student) with "Unknown Student"
    - Return list of attendance records with student_name field
    - _Bug_Condition: isBugCondition(request) where request.endpoint == "/attendance/" AND response.records.missing("student_name")_
    - _Expected_Behavior: expectedBehavior(response) from design - response includes student_name field with full name_
    - _Preservation: Preservation Requirements from design - POST /attendance/ continues to work_
    - _Requirements: 2.6, 2.7_

  - [ ] 3.3 Update `backend/app/schemas.py` - Add student_name field to AttendanceResponse
    - Add `student_name: Optional[str] = None` to `AttendanceResponse` schema
    - This allows backward compatibility if student is deleted
    - _Requirements: 2.6, 2.7_

  - [ ] 3.4 Update `backend/app/schemas.py` - Create DashboardStatsResponse schema
    - Create new Pydantic model `DashboardStatsResponse` with all required fields:
      - `totalStudents: int`
      - `totalClasses: int`
      - `totalSessions: int`
      - `totalAttendance: int`
      - `totalSessionsToday: int`
      - `presentToday: int`
      - `absentToday: int`
      - `attendancePercentage: float`
      - `weeklyAttendance: List[dict]` (array of objects with name, present, absent)
      - `monthlyAttendanceRate: List[dict]` (array of objects with name, rate)
      - `presentVsAbsent: List[dict]` (array of objects with name, value)
    - Add field validators if needed for data validation
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [ ] 3.5 Update `backend/app/api/routers/stats.py` - Update response model
    - Add `response_model=schemas.DashboardStatsResponse` to the `@router.get("/")` decorator
    - This ensures type validation and API documentation
    - _Requirements: 2.1_

  - [ ] 3.6 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Dashboard Stats Complete Fields and Attendance with Student Names
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify dashboard stats response includes all 10+ required fields
    - Verify attendance records include student_name field
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.7_

  - [ ] 3.7 Verify preservation tests still pass
    - **Property 2: Preservation** - Existing CRUD Operations and Authentication
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions):
      - Students CRUD operations work identically
      - Classes CRUD operations work identically
      - Sessions CRUD operations work identically
      - Attendance POST creates records successfully
      - CSV export generates files correctly
      - Authentication endpoints function correctly
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [ ] 4. Checkpoint - Ensure all tests pass
  - Run all tests (exploration + preservation) to verify complete fix
  - Verify no regressions in existing functionality
  - Test edge cases:
    - Empty database (no sessions, no attendance)
    - No sessions today
    - Deleted students with attendance records
  - Verify API documentation is updated with new response models
  - Ask the user if questions arise or if additional testing is needed
